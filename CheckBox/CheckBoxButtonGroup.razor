@inherits CheckBoxGroup
已选中数据：@string.Join(",", SelectedValues)
<br />
<ElCheckbox TValue="object" Model="true" Status="@Status" StatusChanged="status=>SelectAll(status)">全�</ElCheckbox>
<ElCheckboxGroup TValue="List<int>">
    @foreach (var item in Values)
    {
        <ElCheckboxButton TValue="object" Model="item" Status="SelectedValues.Contains(item) ? Status.Checked:Status.UnChecked" StatusChanged="status=> ChangeStatus(status,item)">@item</ElCheckboxButton>
    }
</ElCheckboxGroup>
